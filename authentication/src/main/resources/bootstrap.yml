server:
  port: 8081

spring:
  main:
    banner-mode: "off"
  application:
    name: @artifactId@
  security:
    support:
      override-http-security: false
      audit:
        type: elasticsearch
  elasticsearch:
    rest:
      uris: http://localhost:9200
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yml
        shared-configs:
          - data-id: application.${spring.cloud.nacos.config.file-extension}
            refresh: true